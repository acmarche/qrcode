<fieldset class="scheduler-border">
    <legend class="scheduler-border">Données bancaire</legend>
    {{ form_row(form.bankAccount) }}
    {{ form_row(form.for) }}
    {{ form_row(form.amount) }}
    {{ form_row(form.subject) }}
</fieldset>