<h3 class="text-success">Aperçu</h3>
<div class="d-flex flex-column gap-3">
    {% if imagePath %}
        <img src="{{ imagePath }}" alt="qrcode" width="550">
        <h4 class="text-success mb-4">
            Essayer le Qr Code avec smartphone !
        </h4>
        <a href="{{ imagePath }}" download="download" class="btn btn-primary mt-2 mb-2">
            <i class="fas fa-download" aria-hidden="true"></i>
            Télécharger QR Code
        </a>

        <p>{{ qrString }}</p>
    {% endif %}
</div>